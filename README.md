ESP32 NTP Wi-Fi Clock with OLED Display
Проект компактных часов на базе ESP32, которые синхронизируют время через интернет (NTP) при запуске, а затем работают в автономном режиме для экономии энергии. 
Дисплей включается только по нажатию кнопки.🚀
ОсобенностиАвтоматическая синхронизация: 
  Получает точное время через Wi-Fi с серверов NTP.
  Энергоэффективность: После синхронизации модуль Wi-Fi полностью отключается.
  Режим "по запросу": Дисплей активируется нажатием кнопки на 3 секунды (настраиваемо), после чего гаснет.
  Автономный ход: Время инкрементируется программно с помощью таймера после отключения от сети
🛠 Компоненты
Контроллер: ESP32 (или ESP8266 с минимальными правками библиотек).
Дисплей: OLED 128x32 (контроллер SSD1306, интерфейс I2C).
Кнопка: Тактовая кнопка для активации экрана.
Схема подключенияКомпонентПин ESP32ПримечаниеOLED VCC3.3VПитаниеOLED GNDGNDЗемляOLED SCLGPIO 22 (по умолчанию)I2C ClockOLED SDAGPIO 21 (по умолчанию)I2C DataКнопкаGPIO 1Один контакт на пин, второй на VCCПримечание: В коде используется pinMode(1, INPUT). Убедитесь, что ваша кнопка имеет подтягивающий резистор, либо измените на INPUT_PULLUP и инвертируйте логику, если подключаете к GND.⚙️ Настройка перед прошивкойВ коде необходимо изменить следующие параметры:Данные Wi-Fi:C++const char* ssid = "ВАШ_SSID"; 
const char* password = "ВАШ_ПАРОЛЬ";
Часовой пояс:По умолчанию установлено UTC+3 (Москва/Киев).C++const long gmtOffset_sec = 10800; // 3 часа * 3600 секунд
Время работы экрана:Измените disOnSec, чтобы экран горел дольше или меньше.C++#define disOnSec 3 // Секунды
📦 Необходимые библиотекиДля работы кода должны быть установлены следующие библиотеки в Arduino IDE:Adafruit_GFXAdafruit_SSD1306WiFi (встроена в ядро ESP32)📝 Как это работаетSetup: Инициализируется дисплей и запускается функция timeSync().Синхронизация: ESP32 подключается к Wi-Fi, запрашивает пакеты с time.nist.gov, записывает время в переменные h, m, s и отключает Wi-Fi.Loop: * Каждую секунду срабатывает счетчик timeTick(), обновляющий время внутри программы.Контроллер проверяет нажатие кнопки. Если кнопка нажата, вызывается printLocalTime() и запускается таймер обратного отсчета для работы экрана.По истечении времени disOnSec экран очищается.
